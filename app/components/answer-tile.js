import Ember from 'ember';

export default Ember.Component.extend({
  // upvotes: Ember.computed('answer.upvotes', function() {
  //   return this.set('answer.upvotes', (this.get('answer.upvotes') + 1));
  // }),
});
